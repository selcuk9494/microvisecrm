{
  "name": "istakip",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "vercel-build": "cd packages/web && npm install --workspaces=false --no-audit --no-fund && npm run build"
  }
}
